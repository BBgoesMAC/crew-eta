# Kopieren nach .env und ein eigenes, langes Passwort setzen.
# Solange der Default "changeme-in-env" gesetzt ist, ist der Admin-Bereich
# komplett deaktiviert (HTTP 503) — es MUSS ein eigenes Passwort gesetzt werden.
ADMIN_PASSWORD=changeme-in-env
